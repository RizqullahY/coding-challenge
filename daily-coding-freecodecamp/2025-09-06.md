```py
def rotate(matrix):
    n = len(matrix)
    # bikin matrix baru hasil rotasi
    return [[matrix[n - j - 1][i] for j in range(n)] for i in range(n)]


# contoh
m = [[1, 2], [3, 4]]
print(rotate(m))  # [[3, 1], [4, 2]]
```
