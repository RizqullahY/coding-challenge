```py
def rgb_to_hex(rgb):
    # Ambil angka dari string, misal "rgb(173, 216, 230)" -> [173, 216, 230]
    r, g, b = map(int, rgb.strip("rgb()").split(","))

    # Format ke hex 2 digit per komponen
    return "#{:02x}{:02x}{:02x}".format(r, g, b)

```
